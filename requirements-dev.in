# Development extras (notebooks, exploration)
# Install locally with: pip-compile requirements-dev.in && pip-sync requirements-dev.txt
# NOT installed in CI

# Inherit production dependencies
-r requirements.in

# Notebook + exploration
jupyter
matplotlib

# Tooling
pip-tools

# Azure
azure-storage-blob
azure-identity
azure-keyvault-secrets
azure-ai-ml